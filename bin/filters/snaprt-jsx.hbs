// ======================================================================
/*
  __snaprt.jsx

  {{site.copyright}}

  Shared runtime dependencies of {{generator.agent.name}} client and
  server scripts for input route '{{page.primaryRoute}}'.

  JSX authors should require __snaprt into scope in order to gain
  access to theme bindings (array of named function points to React
  JS components specific to the the current snapsite theme).

  Produced by Encapsule/{{generator.agent.name}} v{{generator.agent.version}} {{generator.build.date}}
  Site build instance: [{{generator.build.time}} {{generator.build.hash}}]
*/
// ======================================================================

module.exports = require('{{snapsiteModule}}');
