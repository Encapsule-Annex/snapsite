// ======================================================================
/*
  __snaprt.jsx

  {{site.copyright}}

  Shared runtime dependencies of {{generator.agent.name}} client and
  server scripts route '{{page.primaryRoute}}'

  Produced by Encapsule/{{generator.agent.name}} v{{generator.agent.version}} {{generator.build.date}}
  Site build instance: [{{generator.build.time}} {{generator.build.hash}}]
*/
// ======================================================================

var snaprt = require('{{snapsiteModule}}');
snaprt.generator = {
    build: {
        time: '{{generator.build.time}}',
        date: '{{generator.build.date}}'
    },
    agent: {
        name: '{{generator.agent.name}}',
        version: '{{generator.agent.version}}'
    }
};
module.exports = snaprt;
